# First, view the current broken file
cat package.json

# If it's corrupted, delete and recreate it
rm package.json

# Create a new valid package.json
cat > package.json << 'EOF'
{
  "name": "fraudshield-ai",
  "version": "1.0.0",
  "description": "AI-Based Financial Fraud Detection System",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "recharts": "^2.8.0",
    "lucide-react": "^0.263.1",
    "axios": "^1.4.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
EOF
